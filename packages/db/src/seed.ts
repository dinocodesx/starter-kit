import { seed } from "./seeds/index";
import { prisma } from "./client";

seed()
  .catch((e) => {
    console.error(e);
    process.exit(1);
  })
  .finally(async () => {
    await prisma.$disconnect();
  });
